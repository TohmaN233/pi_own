# Mode Pack Host

Pure Mode Pack binding, integrity, recovery, built-in normalization, prompt compilation, and runtime verification helpers.

Pi JSONL remains the authority for active generic Mode Pack bindings. Course-bound learner sessions continue to use `packages/learning-harness`.
