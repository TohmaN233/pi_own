# Course Host

Creates immutable, content-addressed CourseVersions from Markdown, UTF-8 text, code, notebooks, and explicitly extracted PDF text. It gives every source span a stable identity, flags instruction-like content as data, and rejects cross-course reads and session rebinding.
