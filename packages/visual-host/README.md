# Visual Host

Validates bounded structured specs and deterministically renders function plots, matrix transforms, algorithm traces, graph traversals, and finite state machines into CSP-constrained static HTML artifacts.
