---
name: curriculum-continuity
description: Plan across lessons using observed progress and progressively richer concept revisits.
---

# Curriculum continuity

Plan across lessons from actual state. Before proposing a substantial next lesson, read completed work and the durable Timeline when available. An approved plan records intent, not proof that learners completed or understood it. If progress evidence is unavailable, state that limitation and plan provisionally.

## Course progression

Map the concept dependency chain and identify load-bearing prerequisites, notation dependencies and likely bottlenecks. Preserve the teacher's goals, calendar and source boundaries. Ask only for missing constraints that would materially change sequence, assessment or workload; do not impose an unnecessary multi-round interview.

Make each lesson sufficiently self-contained to teach, while connecting it to earlier evidence and later use. Place a compact retrieval hook where prior learning matters. Avoid spending most of a new lesson retelling the old one.

## Spiral revisits

For every planned revisit, define a Spiral Contract:

- concept being revisited;
- prior encounter and actual evidence, if available;
- what remains stable;
- what becomes harder or more connected;
- new representation, relationship, abstraction, formalization, transfer distance or boundary;
- learner task;
- evidence that will demonstrate the new level.

Use one or more explicit progression dimensions: complexity, relationship, abstraction, formalization, representation, transfer or boundary. A new title, different numbers or the same explanation later in the calendar does not constitute progression.

Choose whether to keep the earlier model, add a layer, reorganize it or transfer it to a new domain. Surface a prior simplification when it stops being valid. Let the new task require the added structure.

## Runtime boundaries

In Course Builder, use `state`, approved semester revisions, prerequisites, `courseGoalsCovered`, `revisits` and earlier lesson drafts as planning evidence. The current tool does not expose a learner Timeline, so never claim that it does. In learner modes, use available Host tools for durable observations; conversation memory alone is not the source of truth.

Before saving, check for skipped prerequisites, mechanical repetition, overloaded sessions, goals that disappear across the term and revisits with no changed performance. Record gaps rather than fabricating history.

Adapted for Pi Own from OpenMAIC's `curriculum-planner` and `spiral-curriculum` Skills at commit `1e10f60b151cedb59ac21ddbcceb5ee0eed9c984` (MIT).
