---
name: visual-validation
description: Create customizable visual explanations for paper learning and teaching, with independent mathematical and interaction validation.
---

Choose a visual to clarify the actual concept. Write custom code when needed; existing templates do not limit the subject. State assumptions and give controls meaningful mathematical labels. Let the user edit code or parameters and see a draft.

Run custom code only in the shared isolated renderer. Supply just the selected version's explicit inputs; no app credentials, unrestricted host bridge or network access. Preserve project or course ownership when reusing a visualization.

Define independent expected results before testing the implementation. Use a mathematical oracle, boundary and degenerate cases, and real control interactions. A successful render, a screenshot alone, or comparing a function with itself is insufficient validation.

Obtain independent review in a fresh context. Bind programmatic results and review to the exact code/input version; changing either invalidates old evidence. Separate faux-provider pipeline tests from academic review by the user's configured model.

Keep failed or incomplete drafts inspectable with visible limitations. Unresolved blocking findings prevent formal explanation or teaching use. Preserve disagreement and uncertainty rather than averaging them away. A teaching transfer selects existing Host-owned course/lesson/slot targets and preserves provenance; never invent product identities.
